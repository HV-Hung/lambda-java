name: Package Java projects

on:
  push:
   
jobs:
  upload-artifact:
    runs-on: ubuntu-latest
    name: Upload artifact
    
    steps:
      - name: create secrets file
        run: |
          echo "${{ secrets.TEST_SECRET }}" > secret.txt
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: secret
          path: |
            secret.txt
